import React from "react";
import { Link } from "react-router-dom";

const ReorderNavLink: React.FC = () => {
  return <Link to="/admin/projects-reorder">Reorder Projects</Link>;
};

export default ReorderNavLink;
